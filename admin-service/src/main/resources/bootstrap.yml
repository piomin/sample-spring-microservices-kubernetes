spring:
  application:
    name: admin
  cloud:
    kubernetes:
      discovery:
# enable if your admin is deployed in the different namespace then microservices
#        all-namespaces: true
        service-labels:
          spring-boot: true