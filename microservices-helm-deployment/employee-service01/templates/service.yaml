apiVersion: v1
kind: Service
metadata:
  name: {{ include "employee-service01.fullname" . }}
  labels:
    {{- include "employee-service01.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "employee-service01.selectorLabels" . | nindent 4 }}
