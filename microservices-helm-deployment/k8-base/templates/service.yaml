apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8-base.fullname" . }}
  labels:
    {{- include "k8-base.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k8-base.selectorLabels" . | nindent 4 }}
